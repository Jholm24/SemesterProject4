@page "/"
@rendermode InteractiveServer
@inject NavigationManager Nav
@inject RoleState RoleState

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo(RoleState.CurrentRole == "Manager" ? "/manager/dashboard" : "/operator/lines");
    }
}
