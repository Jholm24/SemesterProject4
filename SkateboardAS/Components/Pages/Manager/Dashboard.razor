@page "/manager/dashboard"

<h1>Manager Dashboard</h1>
<p>Discovered machine components:</p>

@foreach (var card in componentCards)
{
    <MachineCard CardModel="@card" OnAction="HandleAction" />
}

@code {
    private List<Core.Models.ComponentCardModel> componentCards = new();

    protected override async Task OnInitializedAsync()
    {
        // TODO: Implement - fetch component metadata from ComponentDiscoveryController
        await Task.CompletedTask;
    }

    private async Task HandleAction(string action)
    {
        // TODO: Implement - dispatch action to appropriate controller
        await Task.CompletedTask;
    }
}
